rantint(a, b)       # Случайное число из отрезка [a, b]
sample(a, b, count) # Список случайных чисел из [a, b] длиной count
choice(lst)         # Выбор случайного элемента списка lst
shuffle(lst)        # Случайная перестановка элементов списка lst
# Ввод:
# Вывод:
# Ответ:
