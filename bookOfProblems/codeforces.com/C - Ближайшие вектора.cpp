#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
typedef unsigned long long ull;
typedef vector<int> vi;
typedef pair<int, int> pi;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    // freopen("input.txt", "r", stdin);
    // freopen("output.txt", "w", stdout);

    int n;
    std::cin >> m;
    int x, y;
    std::cin >> x >> y;

    return 0;
}

// Задача C. Ближайшие вектора
// *****
// Неориентированный угол - это неотрицательная величина, минимальный из двух углов от одного вектора до другого по и против часовой стрелки. Это значение всегда от 0 до PI. Например, два противонаправленных вектора имею угол между собой равный PI. 
